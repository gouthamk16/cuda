# Fluid dynammic simulation using CUDA

Fluid as a velocity field on a grid, 
Velocity is a 2D vector *V* = [u, v]

Fluid Simulation:
* Modify velocity values (eg., add gravity)
* Make the fluid incompressible (projection)
* Move the velocity field (advection)

